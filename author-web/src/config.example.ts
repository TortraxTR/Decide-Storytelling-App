// Copy this file to config.ts and fill in your values.
// config.ts is gitignored — never commit actual URLs or secrets.

export const API_BASE_URL = 'https://YOUR_APP_RUNNER_URL.awsapprunner.com';
export const S3_BUCKET_URL = 'https://YOUR_BUCKET.s3.REGION.amazonaws.com';
