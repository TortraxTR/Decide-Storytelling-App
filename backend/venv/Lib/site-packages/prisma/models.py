# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class Story(bases.BaseStory):
    """Represents a Story record"""

    id: _str
    title: _str
    description: Optional[_str] = None
    thumbnail: Optional[_str] = None
    authorId: _str
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    episodes: Optional[List['models.Episode']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.StoryKeys']] = None,
        exclude: Optional[Iterable['types.StoryKeys']] = None,
        required: Optional[Iterable['types.StoryKeys']] = None,
        optional: Optional[Iterable['types.StoryKeys']] = None,
        relations: Optional[Mapping['types.StoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.StoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Story_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Story_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Story_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Story_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Story_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Story_relational_fields:
                        raise errors.UnknownRelationalFieldError('Story', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Story / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Story',
            }
        )
        _created_partial_types.add(name)


class Episode(bases.BaseEpisode):
    """Represents a Episode record"""

    id: _str
    storyId: _str
    title: _str
    description: Optional[_str] = None
    thumbnail: Optional[_str] = None
    authorId: _str
    order: _int
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    story: Optional['models.Story'] = None
    EpisodeNode: Optional[List['models.EpisodeNode']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.EpisodeKeys']] = None,
        exclude: Optional[Iterable['types.EpisodeKeys']] = None,
        required: Optional[Iterable['types.EpisodeKeys']] = None,
        optional: Optional[Iterable['types.EpisodeKeys']] = None,
        relations: Optional[Mapping['types.EpisodeRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.EpisodeKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Episode_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Episode_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Episode_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Episode_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Episode_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Episode_relational_fields:
                        raise errors.UnknownRelationalFieldError('Episode', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Episode / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Episode',
            }
        )
        _created_partial_types.add(name)


class EpisodeNode(bases.BaseEpisodeNode):
    """Represents a EpisodeNode record"""

    id: _str
    episodeId: _str
    contentUrl: _str
    isStart: _bool
    isEnd: _bool
    Decision_Decision_sourceNodeIdToEpisodeNode: Optional[List['models.Decision']] = None
    Decision_Decision_targetNodeIdToEpisodeNode: Optional[List['models.Decision']] = None
    Episode: Optional['models.Episode'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.EpisodeNodeKeys']] = None,
        exclude: Optional[Iterable['types.EpisodeNodeKeys']] = None,
        required: Optional[Iterable['types.EpisodeNodeKeys']] = None,
        optional: Optional[Iterable['types.EpisodeNodeKeys']] = None,
        relations: Optional[Mapping['types.EpisodeNodeRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.EpisodeNodeKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _EpisodeNode_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _EpisodeNode_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _EpisodeNode_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _EpisodeNode_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _EpisodeNode_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _EpisodeNode_relational_fields:
                        raise errors.UnknownRelationalFieldError('EpisodeNode', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid EpisodeNode / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'EpisodeNode',
            }
        )
        _created_partial_types.add(name)


class Decision(bases.BaseDecision):
    """Represents a Decision record"""

    id: _str
    text: Optional[_str] = None
    sourceNodeId: _str
    targetNodeId: _str
    EpisodeNode_Decision_sourceNodeIdToEpisodeNode: Optional['models.EpisodeNode'] = None
    EpisodeNode_Decision_targetNodeIdToEpisodeNode: Optional['models.EpisodeNode'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DecisionKeys']] = None,
        exclude: Optional[Iterable['types.DecisionKeys']] = None,
        required: Optional[Iterable['types.DecisionKeys']] = None,
        optional: Optional[Iterable['types.DecisionKeys']] = None,
        relations: Optional[Mapping['types.DecisionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DecisionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Decision_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Decision_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Decision_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Decision_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Decision_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Decision_relational_fields:
                        raise errors.UnknownRelationalFieldError('Decision', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Decision / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Decision',
            }
        )
        _created_partial_types.add(name)



_Story_relational_fields: Set[str] = {
        'episodes',
    }
_Story_fields: Dict['types.StoryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('title', {
            'name': 'title',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('thumbnail', {
            'name': 'thumbnail',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('authorId', {
            'name': 'authorId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('episodes', {
            'name': 'episodes',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Episode\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Episode_relational_fields: Set[str] = {
        'story',
        'EpisodeNode',
    }
_Episode_fields: Dict['types.EpisodeKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('storyId', {
            'name': 'storyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('title', {
            'name': 'title',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('thumbnail', {
            'name': 'thumbnail',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('authorId', {
            'name': 'authorId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('order', {
            'name': 'order',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('story', {
            'name': 'story',
            'is_list': False,
            'optional': True,
            'type': 'models.Story',
            'is_relational': True,
            'documentation': None,
        }),
        ('EpisodeNode', {
            'name': 'EpisodeNode',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.EpisodeNode\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_EpisodeNode_relational_fields: Set[str] = {
        'Decision_Decision_sourceNodeIdToEpisodeNode',
        'Decision_Decision_targetNodeIdToEpisodeNode',
        'Episode',
    }
_EpisodeNode_fields: Dict['types.EpisodeNodeKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('episodeId', {
            'name': 'episodeId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('contentUrl', {
            'name': 'contentUrl',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isStart', {
            'name': 'isStart',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('isEnd', {
            'name': 'isEnd',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('Decision_Decision_sourceNodeIdToEpisodeNode', {
            'name': 'Decision_Decision_sourceNodeIdToEpisodeNode',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Decision\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('Decision_Decision_targetNodeIdToEpisodeNode', {
            'name': 'Decision_Decision_targetNodeIdToEpisodeNode',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Decision\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('Episode', {
            'name': 'Episode',
            'is_list': False,
            'optional': True,
            'type': 'models.Episode',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Decision_relational_fields: Set[str] = {
        'EpisodeNode_Decision_sourceNodeIdToEpisodeNode',
        'EpisodeNode_Decision_targetNodeIdToEpisodeNode',
    }
_Decision_fields: Dict['types.DecisionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('text', {
            'name': 'text',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sourceNodeId', {
            'name': 'sourceNodeId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('targetNodeId', {
            'name': 'targetNodeId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('EpisodeNode_Decision_sourceNodeIdToEpisodeNode', {
            'name': 'EpisodeNode_Decision_sourceNodeIdToEpisodeNode',
            'is_list': False,
            'optional': True,
            'type': 'models.EpisodeNode',
            'is_relational': True,
            'documentation': None,
        }),
        ('EpisodeNode_Decision_targetNodeIdToEpisodeNode', {
            'name': 'EpisodeNode_Decision_targetNodeIdToEpisodeNode',
            'is_list': False,
            'optional': True,
            'type': 'models.EpisodeNode',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(Story)
model_rebuild(Episode)
model_rebuild(EpisodeNode)
model_rebuild(Decision)
